// create main div
let container = document.createElement("div");
container.id = "container";

// styling for container
container.style.cssText = "width: 80%; hight: 80vh; marg"















// add container to body
document.body.prepend(container)